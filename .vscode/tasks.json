{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Boot current file (Make)",
      "type": "shell",
      "options": { "cwd": "${workspaceFolder}/L2IP/sw" },
      "command": "make boot FILE='${file}'",
      "problemMatcher": []
    }
  ]
}
